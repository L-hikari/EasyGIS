/** @type {import("next").Metadata} */
export const metadata = {
    keywords: 'vector data, vector, vector data preview',
    description: 'Input vector data and preview on the map'
};

export default function Layout({children}) {
    return <>{children}</>;
}

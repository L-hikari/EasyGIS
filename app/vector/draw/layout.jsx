/** @type {import("next").Metadata} */
export const metadata = {
    keywords: 'draw geometry, draw polygon, get coordinate',
    description: 'Draw geometry and get the coordinate'
};

export default function Layout({children}) {
    return <>{children}</>;
}

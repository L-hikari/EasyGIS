/** @type {import("next").Metadata} */
export const metadata = {
    keywords: 'wms, wmts, tms, ogc, raster layer',
    description: 'Raster Layers Preview'
};

export default function Layout({children}) {
    return <>{children}</>;
}
